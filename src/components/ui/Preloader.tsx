// Preloader intentionally disabled.
// The full-screen blocking loading screen has been removed.
// The 3D canvases each manage their own in-place loading states.
export default function Preloader() {
  return null;
}
